[project]
name = "pypacking"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "debugpy",
  "hatchling"
]

[tool.hatch.build]
artifacts = [
  "src/pypacking/schemas/*.json"
]

[build-system]
requires = [ "hatchling" ]
build-backend = "hatchling.build"

