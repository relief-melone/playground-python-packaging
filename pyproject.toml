[project]
name = "pypacking"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "debugpy",
  "hatchling"
]

[tool.hatch.build]
include = [
  "src/schemas/*.json",
  "src/pypacking/*"
]

[build-system]
requires = [ "hatchling" ]
build-backend = "hatchling.build"

